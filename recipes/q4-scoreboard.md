# Recipe — לוח התוצאות של תוכנית רבעון 4 (לקוחות קיימים)

> השיטה בלבד. הסקריפט (runtime): `gt-factory-os/scripts/sales-report/q4_scoreboard.py`
> — **לעולם לא בריפו הזה.** התוכנית שהוא קורא:
> `Sales-Machine/evidence/2026-08-31-q4-daily-plan.csv`.
> נבנה 2026-08-31. אומת חי מול Shopify באותו יום.

## פקודה אחת

```bash
cd gt-factory-os/scripts/sales-report
python3 q4_scoreboard.py                 # מיום ראשון האחרון
python3 q4_scoreboard.py --since 2026-09-07
python3 q4_scoreboard.py --selfcheck     # אופליין, בלי רשת — 9 בדיקות
```

זה התנאי של D6: המספר השבועי **נוצר מחדש מהמקור בפקודה אחת**. אין גיליון מתוחזק ביד.

## שני מספרים, בכוונה

| | מקור | מה זה |
|---|---|---|
| **אות** | Shopify — ה-SKU של המהלך הופיע בהזמנה בחלון | **לא זכייה.** חשבון `עומק` כבר קונה מהמשפחה הזאת; הופעה בהזמנה לא מוכיחה כלום |
| **זכייה** | עמודת `תוצאה` ב-CSV, מסומן `זכה` על ידי אדם | הקצב השנתי נספר **רק** מזה |

הבחנה זו היא התיקון היחיד ששיטת המדידה מחייבת. בלעדיה כל חשבון עומק נספר כזכייה
בשבוע הראשון, והיעד היה נראה מושג ביום שהוא נכתב.
המכונה סופרת. האדם מאשר. אות בלי תוצאה רשומה מסומן כאזהרה, לא מקודם.

## הבסיס הכספי (נעול, זהה לטבלת העובדות)

- שורת מוצר = `lineItems.discountedTotalSet.shopMoney` = המחיר השמור = **ללא מע"מ** (תום 05/08/2026).
- `customer.amountSpent` ו-`average_order_value` של ShopifyQL — **אסורים** (אנומליות מתועדות; חשבון אחד עם 58 הזמנות מול ₪0.00).
- עמודות המס/net/gross של ShopifyQL — **אסורות**: שופיפיי מוגדרת `taxesIncluded=true @17%` בטעות.
- שיוך חודש ושבוע לפי **Asia/Jerusalem**, לא UTC.
- `test:true` מוחרג · `cancelledAt≠null` מוחרג · שניהם נספרים ומדווחים.

## התאמת שמות

אותו כלל בדיוק שבנה את התוכנית (`plan.py`): חפיפת אסימונים אחרי הסרת `בע"מ` וגרסאותיו,
עם בונוס להכלה מלאה של הקבוצה הקצרה, סף `0.99`. סניף של רשת מתמפה לרשת שלו.
הזמנות מלקוח שלא הותאם **מדווחות במפורש** — לא נבלעות.

## מה מדווח

פעילות: מגעים שתוכננו בחלון · מגעים עם תוצאה רשומה, לפי בעלים.
כסף: הזמנות בחלון · חשבונות מהתוכנית שהזמינו · אותות · זכיות מאושרות ·
קצב שנתי שנזכה בו · מוזה שנסגרו מתוך 20 · המרה לפי מהלך ולפי ארכיטיפ.

## איפה זה נקרא

**לא בפגישה חדשה.** תוכנית שצריכה פגישה חדשה כדי לשרוד — לא תשרוד.
- **ראשון** — עם תמונת התזרים והתקציב שתום ואלכס כבר מקבלים.
- **רביעי 13:00** — עשר דקות בפגישה עם אלכס שכבר קיימת.

## אימות (כלל 4)

- `--selfcheck` — 9/9: 153 חשבונות · לכל אחד מהלך · תמהיל `רוחב 76 / עומק 58 / החלפה 19`
  זהה ללוח · 20 חשבונות מוזה · סניף רשת מתמפה לרשת · שם זר לא מתמפה ·
  40 שמות מדויקים מתמפים לעצמם · אין משימה בשישי/שבת · אין משימה בלי בעלים או תאריך.
- הרצה חיה ראשונה: 2026-08-31, חלון `--since 2026-08-24`. 87 הזמנות, 35/153 חשבונות הזמינו,
  5 אותות, 0 זכיות מאושרות. זהו קו הבסיס לפני שהתוכנית התחילה.

## מגבלה ידועה

חצי הפעילות (מגעים, תשובות) **הוא קלט אנושי מטבעו** — הודעת וואטסאפ שנשלחה
לא נמצאת בשום מערכת ש-GT מחזיקה. עמודת `תוצאה` ב-CSV היא המקור.
ריקה = לא נמדד, וכך זה מדווח. לא ניחוש, לא אפס מוסווה.
