# Doctrine — Decisions Log

> Only Tom decides. `CONFIRMED` = Tom said it explicitly (date + context).
> `PROPOSED` = recommended by Claude, awaiting Tom's word — **not policy yet**.

| ID | Decision | Status | Date | Context |
|---|---|---|---|---|
| D-001 | The sales machine's knowledge/agents layer lives in this dedicated repo (`Sales-Machine`), separate from PRODUCTION and from runtime repos. | **CONFIRMED** (Tom) | 2026-07-18 | Tom opened the repo and directed the knowledge-first build in it. |
| D-002 | Build order is knowledge-first: constitution → seed verified knowledge → truth interviews → system reconciliation → agents (read-only first) → automations last. | **CONFIRMED** (Tom) | 2026-07-18 | Tom approved the 5-phase plan ("נשמע מצוין") with the explicit goal: 100% accurate, current, correct before building. |
| D-003 | The website (any variant: storefront work, marketing site, B2B portal) is **not** built in this repo. It gets its own runtime repo when built; its content/brief (core story, ICP, messaging) is authored here in `doctrine/`. | PROPOSED | 2026-07-18 | Keeps the brain/runtime split that already works (PRODUCTION vs. portal). Site scope itself is U-008. |
| D-004 | No Ruflo / claude-flow framework layer in this repo. Native tooling (Claude Code agents, workflows, skills, this repo's registry) covers the need with full auditability. | PROPOSED | 2026-07-18 | Grounded in GT's own evaluation (TEST-GT-START `docs/ruflo-session-final-report.md`, 2026-05-22): verdict "not ready for another repo", auto-hooks on every message, `auto-commit.sh`, writes outside the repo, unrequested MCP servers, untrustworthy verify summary — conflicts with truth rules 1/3/5. Revisit only if upstream blocking issues close AND a concrete gap native tooling can't fill is named. |
| D-005 | Customer-facing outreach automation stays behind `SALES_CUSTOMER_OUTREACH_WRITE_ENABLED` (default `false`) + Tom written approval + dry-run + ≥24h soak. | CONFIRMED (inherited) | 2026-07-18 | Module declaration §11 (PR #46); PRODUCTION External-action authorization. |
| D-006 | **D10 is reversed: Make MAY carry Facebook leads into `sales_core`, as transport only.** Meta's Graph API is unreachable for Tom — he holds no Meta developer access, the existing Business app is WhatsApp-only and he is not its admin, and developer registration is blocked at SMS verification. Make is an approved Meta partner whose own app already holds `leads_retrieval`, so a single OAuth click restores intake with no developer access at all. **Scope of the reversal is deliberately narrow:** Make fetches the lead and POSTs it to our `/ingest` route. Storage, phone normalisation, org matching, deduplication, alerting, the conversion loop and the heartbeat all stay in `sales-leads-poll`. Make holds no data and makes no decisions. | **CONFIRMED** (Tom) | 2026-08-24 | Tom said "MAKE." in writing after I stated the objection in full and asked him to choose. The objection stands on the record: D10 existed because Make's silently-expiring OAuth token is what killed the pipeline on 2026-06-07 — verified again today, connection `gteveryday` (id 6309050) expired `2026-06-07T20:37:12Z`, the exact hour leads stopped. What changes the risk is not Make becoming safer but the failure becoming *loud*: the daily heartbeat (D11) alarms within 24h of silence, and it was proven working on 2026-08-24 04:00Z. The 2026-06-07 failure cost two months of silence; the same failure now costs one day. Direct Graph API access remains the preferred end state — revisit when Tom or Alex obtains app admin rights. |
| D-007 | **Lead intake moves from Meta lead forms to Click-to-WhatsApp — into the WhatsApp Business APP on GT's existing work number, NOT the Cloud API.** The ad opens a WhatsApp conversation; the lead initiates; attribution comes from the **per-ad pre-filled message**, not from a `referral` webhook. The number is already a WhatsApp Business account with a catalog, and is **not** the Dualhook order-intake number. **What carries over unchanged:** `sales_core` storage, the append-only `lead_event` ledger, the portal `(sales)` workspace, the conversion job, and the `/ingest` route — whose own contract already names WhatsApp as an accepted caller and derives a stable `external_id`, so a retried POST is idempotent. **What this decision deletes:** the Cloud-API migration, the `referral`/`ctwa_clid` handler, the whole template stage (5.1–5.3) and its operational budget, and — critically — the Meta developer-access blocker that D-006 records, because CTWA runs against an APP number and needs only Page admin plus ad-account access, both of which GT plainly has since ads are running today. **What it costs:** no automatic lead capture from the app (Meta exposes no API to it, and unofficial readers get the number permanently banned), so the record layer is a phone shortcut plus an optional link in the auto-greeting, both POSTing to `/ingest`. | **CONFIRMED** (Tom) | 2026-08-30 | Tom directed it in writing: "מה שנעשה זה נשים על הטלפון שלי- שלא מחובר לDUALHOOK את הפנייה ישירות מהקמפיין לווצאפ", then "זה המספר של העבודה שלי ויש עליו כבר ווצאפ ביזנס עם קטלוג והכל". Source plan (Alexander, proposal input only): `evidence/2026-08-30-ctwa-plan-alexander-source.md`. Assessment and the amendment recording this decision: `evidence/2026-08-30-ctwa-plan-impact.md`. Runbook: published execution map, 4 blocks / 19 steps. The earlier draft of this row proposed the Cloud-API route and listed Meta Business Manager admin as a precondition; Tom's decision routes around that entirely, which is why it is recorded here rather than left as the objection. Stage 7 of the plan (automated customer-facing sends) is **not** enabled by this decision and stays behind D-005 — nothing in the app path sends anything from our code, so `SALES_CUSTOMER_OUTREACH_WRITE_ENABLED` is untouched at `false`. |
| D-008 | **CORRECTED 2026-08-30. Retire the Make lead-transport scenario when the lead-form campaign is paused — not before, and not by not building it.** The original text of this row proposed *not building* it. That was written from a stale `CURRENT_STATE.md` line and is factually wrong: `GT Sales — lead intake → /ingest` (Make scenario 7075235) was built by Tom on 2026-08-24 and has been carrying leads into `sales_core` every day since — verified in the table, not just in Make's run log (`evidence/2026-08-30-lead-intake-is-live.md`). **Until the form campaign is actually paused it is the only automatic lead capture GT has**, so switching it off early would reopen the exact silence this whole track exists to prevent. The hourly pulse (7075243) is not retired at all — liveness survives the architecture change; under D-007 its input becomes "did the ad deliver and did a lead get recorded" rather than "did Make deliver a lead". | PROPOSED | 2026-08-30 | The correction matters more than the decision. The repo asserted "not built — awaiting Tom's go-ahead"; it was true when written on 2026-08-24 and false a few hours later, and nothing updated it. On 2026-08-30 Claude read that line, believed it, and told Tom his leads were not flowing anywhere — while they were landing daily. `CURRENT_STATE.md` is the sole authority on build status precisely so this cannot happen, which makes this a failure of the freshness discipline (rule 6), not of the rule that assigns authority. Also found in the same pass: a **two-week hole, 2026-08-10 to 2026-08-23**, between the manual export and the scenario going live — roughly 15–20 leads that reached Meta and never reached us, recoverable from Lead Center until early November (90-day retention), and idempotent to re-post because `/ingest` derives a stable `external_id`. Plus 2 items sitting in the scenario's dead-letter queue, not yet inspected. |
